"use client";

/**
 * Wave 1A stub — replaced by Task 23.
 *
 * Renders a placeholder sidebar so CanvasEditorShell mounts without
 * errors. Real tool palette implementation lands in Task 23.
 */
export function Toolbar() {
  return (
    <aside
      className="w-16 shrink-0 border-r border-white/10 bg-zinc-950"
      aria-label="Toolbar"
      data-builder-component="Toolbar"
    />
  );
}
