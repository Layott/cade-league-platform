# Brand assets drop zone

Drop files here. Agents will pick them up when needed for Plan 16 (broadcast motion design) + Plan 14 prompt tuning.

## Layout

- `logos/` — CADE master logo, Division 1 Elite lockup, sponsor logos. SVG preferred, PNG acceptable at 1024px+ with transparent background.
- `videos/` — overlay style reference videos (MP4 or MOV). Any length. Label with what you want to reference in filename, e.g. `scorebar-motion-reference.mp4`, `intro-sequence.mov`.
- `screenshots/` — real FUT/eFootball post-match stats screens for Plan 14 OCR prompt tuning. 5-10 samples cover typical variation.
- `fonts/` — broadcast-specific font files (TTF/OTF) if different from web. Otherwise leave empty and agent uses Space Grotesk / Inter / JetBrains Mono already in `globals.css`.

## Status

Empty. Drop stuff in, then tell Claude "assets ready" and I'll pick them up.
